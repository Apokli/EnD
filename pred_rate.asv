function pr = pred_rate(real, pred)
    pr = zeros(size(pred, 1), size(pred, 3));
    for i = 1 : size(pred, 1)
        for j = 1 : size(pred, 3)
            pr(i, j) = sum(double(pred(i, :, j).' == real && real == ones(size(pred, 2)))) / sum(real);
        end
    end
end

